name: Auto add label

on:
  pull_request:
    branches:
      - '**'
    types: [opened]

jobs:
  label:
    run-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - uses: andymckay/labeler@master
        with:
          add-labels: '👑Priority: Medium🚊, 🚥Status: WIP🚧'

